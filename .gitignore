# -------------------------------------------------------------------
# 1. PYTHON/OS ARTIFACTS
# -------------------------------------------------------------------
__pycache__/
*.pyc
*.pyo
*.pyd
*~
.ipynb_checkpoints/  # Jupyter Notebook autosave files
.vscode/             # VS Code settings (unless shared)

# -------------------------------------------------------------------
# 2. VIRTUAL ENVIRONMENTS
# -------------------------------------------------------------------
# Virtual environment folders
.venv
venv
env

# -------------------------------------------------------------------
# 3. GEMINI/KAGGLE SPECIFIC OUTPUTS & LOGS
# -------------------------------------------------------------------
# The final generated report file (should not be committed before submission)
# This file is created dynamically during the notebook run.
SENTINEL_MISSION_LOG.md

# Log files and runtime artifacts
*.log
*.tmp
temp/

# -------------------------------------------------------------------
# 4. RAG / DATA ARTIFACTS
# -------------------------------------------------------------------
# FAISS vector store indexes (large binary files)
*.bin
*.idx
faiss_index/
chroma.sqlite3 # Example for Chroma DB if you switch RAG implementations
data/embeddings/

# -------------------------------------------------------------------
# 5. SENSITIVE & ENVIRONMENT VARIABLES (CRITICAL)
# -------------------------------------------------------------------
# Local environment variables file (contains API keys)
.env
.flaskenv

# The actual API key itself (never commit this)
google_api_key.txt
api_key.json

# -------------------------------------------------------------------
# 6. PACKAGE BUILD ARTIFACTS
# -------------------------------------------------------------------
# Distribution and build files
/dist/
/build/
*.egg-info/
